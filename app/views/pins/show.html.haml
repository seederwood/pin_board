%h1= @pin.title
%p= @pin.description
%p
 submitted by:
 = @pin.user.email

= link_to "Back", root_path
= link_to "Edit Pin", edit_pin_path
= link_to "Delete Pin", pin_path, method: :delete, data: { confirm: 'Really delete this pin?'}
